server {
    listen 80;
    access_log /var/log/nginx/access.log;
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    location /stub_status {
        stub_status;
        allow 127.0.0.1;
        allow 172.0.0.0/8;
        deny all;
    }
}
